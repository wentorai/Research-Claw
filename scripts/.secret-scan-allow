# secret-scan allowlist — one path glob per line (case-style match against repo-relative paths).
# Only for KNOWN, ACCEPTED exceptions. Keep this list tiny.

# DeepSeek key + Telegram token here were REVOKED 2026-06-14. Kept in history per owner decision
# (no force-push / history rewrite). Already public, now inert — must not block future pushes.
config/openclaw.json.prefix-2026-06-09T0743

# Gate-owned scanner implementation: contains only detection regexes and
# positive/negative self-test strings, never operator credentials.
scripts/acceptance/docker-image-health-smoke.sh

# Fixed self-signed provider key for the isolated T09 TLS probe. The matching
# certificate subject is "Research-Claw T09 Test Only"; test/ is excluded from
# Docker contexts and installer candidate manifests.
test/fixtures/bootstrap-profile-e2e-provider.key.pem
