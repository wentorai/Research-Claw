# This is a fixed, self-signed TLS fixture used only by the isolated T09 local
# provider probe. Its certificate subject is "Research-Claw T09 Test Only";
# Docker and installer candidate manifests exclude the entire test tree.
paths-ignore:
  - "test/fixtures/bootstrap-profile-e2e-provider.key.pem"
