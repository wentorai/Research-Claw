# Research-Claw v0.7.6

> 科研龙虾 v0.7.6 — built as an OpenClaw satellite.
> OpenClaw base: `2026.6.1` · Protocol v4 · Date: 2026-07-27

本次发布集中完成三条能力线：外设与文件工作流、运行可观测性、双模型监督。监督插件同步升级为 `0.2.0`。

## What's New

### 外设与文件工作流

- **实验设备接入**：新增浏览器摄像头、本机摄像头、RTSP 摄像头与 Plaud 录音笔；支持抓帧、预览、观测时间线和设备监控。
- **无人值守监控**：设备监控可按计划运行，异常通过绑定渠道发送并在 Dashboard 留存；正常轮使用 OpenClaw 静音协议，避免无效推送。
- **安全的视频链路**：RTSP 仅允许 `rtsp/rtsps`，HLS 请求携带网关鉴权；不安全的原生 HLS 回退会明确降级。
- **上传与工作区**：文件流式落盘、同名冲突处理、目录拖放、根目录目的地选择，以及聊天图片统一归档到 `sources/chat/images/`。
- **路径与并发防护**：统一工作区路径守卫，修复命令注入、并发写入丢失、临时文件泄漏和设备目录逃逸。

### 双模型监督 0.2.0

- **诚实的异步复审**：主回答发送后异步审查，结果和“建议修正”写入持久化审计并在 Supervisor 面板呈现；不再承诺阻塞或替换已发送文本。
- **危险工具门禁**：确定性规则即时拦截已知危险命令；高风险工具深审默认最多等待 4 秒，超时可观察并放行，不让模型审查无限阻塞主流程。
- **人在环审批**：`approve` 策略接通 Dashboard 原生审批卡，审批状态幂等并可恢复。
- **引用存在性核验**：支持 DOI/arXiv/标题来源交叉核验、Unicode 标题和并发去重；联网策略默认 `off`，避免未授权外发研究内容。
- **会话与协议修复**：使用 OpenClaw 真实 `(event, ctx)` hook 契约和 canonical session key；持久化 review 状态机支持重启恢复、单调游标重放和失败可观察。
- **科研压缩提示**：默认 compaction 指令优先保留研究事实、方法、参数、证据边界和未解决问题；未实现的 Memory Guard 能力已撤下。

### 可观测性与恢复

- **结构化错误**：RPC 保留领域错误码，Dashboard 按失败类型提供重试或恢复动作。
- **后台失败通知**：定时任务和后台运行失败会生成可去重的用户可见通知。
- **运行时自检**：启动时核对插件激活、模型可见工具和注入技能，识别“源码存在但运行时未挂载”的静默失效。
- **诊断与日志**：`diag.sh` 生成键名驱动脱敏的诊断包；原生和 Docker 网关日志持久化，并保留明确的失败取证路径。
- **容器生命周期**：Docker 正确转发停止信号，修复重启等待窗口吞信号的竞态，并在退出时显示告别页。
- **容器发布边界**：镜像默认排除本地工作区、用户配置、子模块历史与示例，仅烘焙启动模板和 PPT 运行脚本。

### 体验改进

- 首次启动显示欢迎卡和示例任务，已完成引导不会重复出现。
- 新增 Kimi K3 模型预设，更新首配默认模型。
- 会话打断只使用 `Esc`，归还 `Ctrl/Cmd+C` 的复制语义。
- 安装与 Docker 退出流程补齐明确的完成页和后续入口。

## 行为边界

- Supervisor 的输出复审是异步建议和审计，不会修改已经交付给用户的消息。
- 引用联网核验默认关闭；外部学术注册表限流或不可用时会标为 `unverifiable`，不会据此断言引用造假。
- macOS 本机摄像头仍受系统 TCC 授权约束；没有授权时会安全超时并回收进程。
- Windows 的 `explorer.exe` 文件打开返回码仍需 Windows 真机补验，不影响 macOS。

## Upgrade

```bash
curl -fsSL https://wentor.ai/install.sh | bash
```

Docker 用户：

```bash
docker pull ghcr.io/wentorai/research-claw:0.7.6
```
